version: 0.0
os: linux
files:
  - source: /bmsconsole.war
    destination: /var/lib/tomcat7/webapps/
  - source: /restart_server.sh
    destination: /home/ubuntu/
hooks:
  ApplicationStart:
    - location: /restart_server.sh
      timeout: 3600

